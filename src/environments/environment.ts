/** Dev: `ng serve` proxies `/api` → Laravel (`php artisan serve` on :8000). See `proxy.conf.json`. */
export const environment = {
  production: false,
  apiUrl: 'https://funny-movies-b3dt.onrender.com/api',
};
